

function what_is_engl() {
  engl is a cli which accepts a .engl file as input and outputs text to the stdout or some other source. It is used to help make sense of software created with the use of large language models. What if natual language was a programming langauge?
}

function main() {
  what_is_engl();
}
